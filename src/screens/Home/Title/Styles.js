import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleHome: {
        color: "#000000",
        fontSize: 60,
        fontWeight: "bold",
        textAlign: "center",
        marginTop: 120,
        marginBottom: 150,
    }
});
