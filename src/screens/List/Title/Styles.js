import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleList: {
        color: "#000000",
        fontSize: 40,
        textAlign: "center",
        marginTop: 150,
        marginBottom: 130,
    }
});
