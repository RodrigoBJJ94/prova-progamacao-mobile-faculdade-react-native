import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleInsert: {
        color: "#000000",
        fontSize: 36,
        textAlign: "center",
        marginTop: 80,
        marginBottom: 90,
    }
});
